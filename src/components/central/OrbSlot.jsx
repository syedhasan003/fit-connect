export default function OrbSlot() {
  return (
    <div
      style={{
        height: 160,
        borderRadius: 20,
        background:
          "radial-gradient(circle at center, rgba(255,255,255,0.1), transparent 70%)",
        opacity: 0.35,
      }}
    />
  );
}
